---
marp: true
theme: gaia
paginate: true
class: invert
style: |
  img {
    display: block;
    margin-left: auto;
    margin-right: auto;
  }
---

# 程式語言、資料結構與演算法

把解決問題的邏輯，寫成電腦能懂、能重複執行的步驟

---

## 前言：從排班表談起

想像你是工廠的生產排程員，要為下週安排工作：3條產線、每天3班、45名操作員、部分人只能操作特定機台、每班至少3名合格人員、還有人請假。

如果沒有電腦：手工排班要花數小時且容易出錯，紙筆紀錄難以管理保存，邏輯也很難傳給下一個新手。

---

## 不懂程式設計，你可能會...

- 排班、記錄、計算全靠手工，耗時又容易出錯
- 大量資料只能用紙本管理，不易保存、難以備份
- 不知道如何把解決問題的邏輯講清楚，難以傳承給他人
- 與 IT／AI 工程師溝通需求時，說不清楚「程式該怎麼判斷」
- 面對「資料量變大系統變慢」的問題，不知道問題出在哪裡

---

## 懂程式設計，你可以...

- 用 **程式語言** 把排班邏輯寫成程式，幾秒鐘產生符合條件的排班表
- 用良好的 **資料結構** 有效率地儲存與存取大量資料
- 用有效率的 **演算法** 計算複雜邏輯，甚至讓 AI 模型自動最佳化
- 與 IT、AI 工程師有效溝通，具備自動化工具的基本應用能力

> **把人類解決問題的邏輯，以電腦能理解的方式寫下來，讓電腦幫你重複執行、高效儲存、快速計算**

---

## 工業場景問題 對應 程式設計概念

| 工業場景中的問題 | 對應的程式設計概念 |
|---|---|
| 記錄每台機器每日產量 | 變數與資料型態 |
| 依序執行品管檢查步驟 | 流程控制（循序、條件、迴圈） |
| 管理數百筆訂單資料 | 資料結構（串列、字典） |
| 計算最短搬運路徑 | 演算法（搜尋、最佳化） |
| 自動產生生產報表 | 函數與模組化設計 |

---

# 第一篇：程式設計與運算思維

---

# 一、問題解決與運算思維

本章的目的不是把你訓練成程式設計師，而是幫助你理解「程式是如何思考問題的」。

掌握運算思維、讀懂基本程式邏輯，讓你在面對數位化工作環境時，能與 IT、AI 工程師有效溝通。

---

## 什麼是運算思維？

**運算思維（Computational Thinking）** 不是叫你「只能寫程式」才能實現，而是一種 **用電腦可以理解的方式來解決問題的思考方法**

- 即使從未寫過一行程式碼，也能幫助你更有系統地分析複雜問題
- 由四個核心能力組成：問題分解、模式辨識、抽象化、演算法思考
- 四者互相支撐，共同構成解決問題的完整框架

---

## 補充小知識：運算思維的推手

- 概念雖可追溯至更早，但真正讓它成為全球教育界重視關鍵推手的，是美國電腦科學家珍妮特·溫（Jeannette Wing）
- 2006年在《ACM 通訊》發表短文，主張運算思維應像讀寫算一樣成為基本素養
- 這個主張深刻影響了許多國家的資訊教育政策

---

## 問題分解（Decomposition）

面對複雜問題，第一步是將它 **拆解成較小、較易處理的子問題**，各自獨立解決後再組合起來

- 蓋大樓不是一個問題，而是地基、結構、水電、裝潢等數十個子問題
- 撰寫工廠管理系統也可拆解為訂單、庫存、品質紀錄、報表產生等模組

**工業應用**：生產線可拆為 原料入庫 → 備料 → 加工A → 加工B → 檢測 → 包裝 → 出貨

---

## 模式辨識（Pattern Recognition）

找出問題中重複出現的規律或結構，套用已知解法，不必每次從零開始

- 「計算生產線日平均產量」與「計算業務員月平均銷售額」結構完全相同
- 都是「對一組數字取平均值」——同一段邏輯能處理兩種情境

**工業應用**：SOP 正是模式辨識的體現——不同機台若遵循相同「夾緊→加工→量測→記錄」模式，就能用同一套框架管理

---

## 抽象化（Abstraction）

忽略問題中不重要的細節，只保留對解決問題真正有用的資訊

- 地圖省略建築物顏色、路旁樹木，只留道路、地名——反而讓導航可行
- 「函數」正是抽象化的實踐：不需要知道 `計算平均值()` 內部如何運作
- 只需要知道「給它一串數字，它會回傳平均值」

---

## 演算法思考（Algorithmic Thinking）

**演算法（Algorithm）** 是解決特定問題的 **一系列明確、有限、有序的步驟**

- **明確性**：每步驟清楚無歧義，不能「大概做一下」
- **有限性**：必須在有限步驟內結束
- **有效性**：每個步驟都是電腦可實際執行的操作
- **輸入與輸出**：有明確輸入，產生預期輸出

**工業應用**：SOP 與演算法本質相近，差別在執行者是人還是電腦

---

## 小結：運算思維四面向

> **分解、模式辨識、抽象化、演算法思考——不是孤立的技巧，而是解決複雜問題時自然流動的思考過程**

- 面對工廠流程改善、系統設計或資料分析任務
- 有意識地運用這四個思維工具，往往能讓問題變得清晰可解

---

# 二、程式設計基本概念

系上的「程式設計」課程會把這一節的概念講得更清楚，這裡建立最基本的概念。

本章介紹程式的本質、變數與資料型態、基本運算——所有程式邏輯的起點。

---

## 程式的本質

一個程式，無論多複雜，其本質都可以用三個字描述：**輸入 → 處理 → 輸出**

```
輸入（Input）  →  處理（Process）  →  輸出（Output）
  原始資料          運算與邏輯          結果與資訊
```

- **輸入**：鍵盤、檔案、感測器訊號、資料庫查詢……
- **處理**：計算、判斷、轉換等操作
- **輸出**：螢幕顯示、檔案寫入、發送指令給機台……

---

## 工業應用：良率計算程式

- **輸入**：今日生產總數（500件）、不良品數（12件）
- **處理**：良率 = (500 − 12) / 500 × 100%
- **輸出**：良率 97.6%，顯示於報表並存入資料庫

> **這個例子雖然簡單，但結構與任何複雜程式完全相同**

---

## 變數與資料型態

**變數（Variable）** 是程式中用來儲存資料的「容器」，有名稱和值——就像倉庫中貼了標籤的儲位

| 資料型態 | 說明 | 工業範例 |
|---|---|---|
| **整數（Integer）** | 沒有小數的數字 | 產品數量、批次編號 |
| **浮點數（Float）** | 帶有小數的數字 | 溫度 23.5°C、良率 0.976 |
| **字串（String）** | 文字資料 | 產品名稱、操作員姓名 |
| **布林（Boolean）** | 真（True）或假（False） | 機台是否運作中 |

---

## 為什麼資料型態重要？

資料型態錯誤是程式常見的錯誤來源

- 若將產品數量儲存為字串「50」而非整數 50
- 計算「50 + 30」時可能得到字串「5030」而非數字 80
- 在工業系統中，這類型別錯誤可能導致產量統計錯誤，造成錯誤的生產決策

---

## 趣味小知識：臭蟲的由來

- 「臭蟲（Bug）」「除錯（Debug）」據說源自1947年
- 葛麗絲·霍普的團隊排查哈佛 Mark II 故障時，真的在繼電器發現一隻被夾死的飛蛾
- 貼在維修日誌上，寫下「第一個發現真正臭蟲的案例」
- 霍普也是最早的程式設計師之一，開發出世界第一個編譯器，並主導設計 COBOL

---

![h:550](images/03-Computer-Program/first-computer-bug.jpg)

---

## 基本運算：算術與邏輯

**算術運算**：`+` `-` `*` `/` `%`（取餘數）

**邏輯運算**（結果為 True 或 False）：

| 運算 | 說明 | 工業範例 |
|---|---|---|
| `AND`（且） | 兩條件都真才為真 | 溫度正常 AND 壓力正常 → 製程正常 |
| `OR`（或） | 任一為真即為真 | 溫度異常 OR 壓力異常 → 觸發警報 |
| `NOT`（非） | 真假反轉 | NOT 合格 → 不合格 |

---

## 工業應用：生產數據計算

```
今日產量    = 500
不良品數    = 12
良品數      = 今日產量 - 不良品數    → 488
良率        = 良品數 / 今日產量      → 0.976
良率百分比  = 良率 * 100             → 97.6%

是否達標    = 良率 >= 0.95           → True（達標）
```

---

# 三、控制結構

若程式只能從頭到尾執行一遍，能做的事情非常有限。

控制結構讓程式能根據條件做出不同選擇、或重複執行步驟，具備真正的「邏輯判斷」與「自動化」能力。

---

## 條件判斷（if-else）

讓程式根據某個條件是否成立，選擇執行不同的程式碼區塊

```
如果 [條件成立]:
    執行 A
否則:
    執行 B
```

```
          ┌─────────────┐
          │  條件判斷？  │
          └──────┬──────┘
         成立    │    不成立
        ┌────────┴────────┐
        ↓                 ↓
   執行 A 區塊        執行 B 區塊
```

---

## 工業應用：品質檢測條件判斷

```
量測值 = 讀取感測器()

如果 量測值 >= 下限 AND 量測值 <= 上限:
    判定 = "合格"
否則如果 量測值 < 下限:
    判定 = "過小，不合格"，觸發警報
否則:
    判定 = "過大，不合格"，觸發警報
```

> **這個邏輯可以嵌入 PLC 程式或 MES 系統，讓機器自動分類產品，無需人工逐一判斷**

---

## 迴圈（for、while）

**迴圈（Loop）** 讓程式重複執行一段程式碼，直到某個條件達成——這是電腦「不知疲倦」的關鍵

**for 迴圈**：適合 **已知重複次數** 的情境

```
對 工單清單 中的每一張工單:
    讀取工單內容、計算所需原料、更新庫存
```

**while 迴圈**：適合 **重複到某條件成立為止** 的情境

```
當 生產線尚未完成今日配額:
    繼續生產，如果原料不足: 發出補料警報
```

---

## 工業應用：批次作業

```
批次清單 = [批次001, 批次002, 批次003, ..., 批次050]

對 批次清單 中的每一個批次:
    投入原料
    執行加工流程
    進行品質量測
    記錄批次結果
    如果良率 < 95%: 發出警告，等候工程師確認
```

---

## break 與 continue

- **break（中斷）**：立即結束整個迴圈，常用於找到目標後不需繼續搜尋
- **continue（繼續）**：跳過本次迴圈剩餘步驟，直接進入下一次迴圈

```
對 感測器資料清單 中的每一筆資料:
    如果 資料 是空值:
        continue    ← 跳過這筆，繼續下一筆
    如果 溫度 > 緊急上限:
        觸發緊急停機
        break       ← 立即停止，進入緊急程序
```

---

## 補充小知識：goto 與結構化程式設計

- 早期語言還有更自由但危險的跳轉方式：`goto`
- 1968年戴克斯特拉發表〈Go To Statement Considered Harmful〉
- 濫用 goto 讓程式邏輯像纏繞的麵條——「義大利麵條式程式碼」
- 這篇文章推動了 **結構化程式設計** 的興起

---

## 小結：控制結構

> **條件判斷讓程式「有邏輯」，迴圈讓程式「有效率」**

- 兩種控制結構的組合，是幾乎所有自動化程式的核心骨架
- 在工業場域中，感測器資料篩選到批次報表生成，都能透過這兩種結構自動化

---

# 四、函數與模組化

把常用的邏輯包裝成可重複呼叫的單元，是程式設計中最重要的抽象化實踐之一。

本章介紹函數的概念，以及模組化設計如何讓大型系統保持可讀、可維護。

---

## 函數的概念

**函數（Function）** 是一段被命名、可重複呼叫的程式碼區塊，接受 **輸入（參數）**，執行處理，回傳 **輸出（回傳值）**

用工廠機台比喻：一台「計算良率的機器」，放入「總產量」「不良品數」，自動輸出「良率百分比」

```
函數定義：
    計算良率(總產量, 不良品數):
        回傳 (總產量-不良品數) / 總產量 * 100

函數呼叫：
    A線良率 = 計算良率(500, 12)    → 97.6
    B線良率 = 計算良率(320,  8)    → 97.5
```

---

## 重複使用與模組化設計

**模組化（Modularization）** 將程式切分為多個功能獨立的函數，每個只負責一件事

- **可讀性**：函數名稱就說明了用途
- **可維護性**：修改一處不影響其他部分
- **可重用性**：同一函數可被不同程式重複呼叫
- **可測試性**：每個函數可獨立測試

---

## 工業應用：生產日報系統

```
函數：讀取感測器資料(機台編號, 日期)
函數：計算良率(總產量, 不良品數)
函數：判斷是否達標(良率, 目標良率)
函數：寄送警報郵件(收件人, 內容)

主程式：
    對 每台機台:
        資料 = 讀取感測器資料(機台, 今天)
        良率 = 計算良率(資料.產量, 資料.不良數)
        如果 NOT 判斷是否達標(良率, 95%):
            寄送警報郵件(工程師, "良率不達標")
```

---

# 五、基本資料處理

實際工作中，我們往往需要處理一組同類型的資料，而非只有單一數值。

本章介紹陣列與列表的基本操作，以及從讀取到輸出的完整資料處理流程。

---

## 陣列與列表（List）

**陣列（Array）** 或 **列表（List）** 依序儲存多個值，並透過 **索引（Index）** 存取特定位置的值

```
每日產量 = [480, 512, 498, 467, 521]
索引:      [0]   [1]   [2]   [3]   [4]   ← 索引從 0 開始

每日產量[0] → 480（星期一）
每日產量[4] → 521（星期五）
```

> **大多數程式語言的索引從 0 開始，是初學者最常見的混淆點之一**

---

## 用迴圈處理整週資料

```
每日產量 = [480, 512, 498, 467, 521]
總產量   = 0

對 每日產量 中的每一個值:
    總產量 = 總產量 + 該值

平均產量 = 總產量 / 5    → 495.6
```

**工業應用**：`感測器讀值`、`工單清單`、`不良原因` 等列表在工業資訊系統中無所不在

---

## 簡單資料讀取與整理：五步驟

1. **讀取資料**：從檔案、資料庫或感測器取得原始數據
2. **篩選（Filtering）**：只取出符合條件的資料
3. **排序（Sorting）**：依特定順序排列
4. **彙整（Aggregation）**：計算總計、平均、最大最小值
5. **輸出（Output）**：寫入報表、存回資料庫、顯示於監控畫面

---

## 範例：感測器資料處理

```
原始資料：溫度紀錄 = [23.1, 24.5, 23.8, 26.2, 23.4, 25.9, 27.1, 23.0]

篩選異常（溫度 > 25.0）：
異常紀錄 = [26.2, 25.9, 27.1]

彙整：
最高溫度 = 27.1　平均溫度 = 24.6　異常次數 = 3
```

---

# 第二篇：資料結構基礎

---

# 六、資料結構的角色

資料結構決定了資料如何被組織、儲存，以及如何被存取。

本章建立整體概念，後續各章逐一介紹列表、堆疊、佇列、映射、樹等常見資料結構。

---

## 為何需要資料結構？

想像一間沒有整理系統的倉庫：所有零件散落各地，每次找零件都要翻遍整個倉庫

**資料結構（Data Structure）** 就是程式中的「倉庫整理系統」

- 同樣是「儲存一批工單」，放進不同資料結構，效率大不相同
- **列表** 可依序處理；**佇列** 確保先到先服務；**映射** 能瞬間查到內容

---

## 抽象資料型態（ADT）

**抽象資料型態（ADT）** 從「使用者角度」定義資料結構：它 **能做什麼操作**，而非內部如何實作

- 就像使用電梯：只需知道「按按鈕，電梯移動到指定樓層」
- 不需要知道纜繩、馬達的運作細節
- 讓設計者專注在「我需要什麼功能」，而非「如何在記憶體中排列資料」

---

## 工業應用：訂單、庫存、工單管理

| 管理場景 | 需要的操作 | 適合的資料結構 |
|---|---|---|
| 訂單等候處理 | 先進先出 | 佇列（Queue） |
| 操作歷史紀錄 | 後進先出 | 堆疊（Stack） |
| 用工單號碼查詢 | 快速依鍵值查找 | 映射（Map） |
| 組織物料清單層級 | 上下層關係 | 樹（Tree） |

---

# 七、陣列與列表（Array / List）

陣列與列表是最基本、最直觀的資料結構。

本章深入說明陣列在記憶體中的儲存方式，以及隨機存取與遍歷兩種核心操作。

---

## 順序儲存概念

陣列元素 **依照順序排列在一起**，每個元素有對應的 **索引** 指示其位置

```
記憶體中的陣列示意：
位置:  [100] [101] [102] [103] [104]
內容:   480   512   498   467   521
索引:   [0]   [1]   [2]   [3]   [4]
```

> **元素通常連續排列，知道第一個位置就能直接算出第 n 個在哪裡——依索引存取的速度極快**

---

## 存取與遍歷

**隨機存取（Random Access）**：直接透過索引取得元素，速度固定不變

```
每日產量[3] → 直接取得第4天的產量 467
```

**遍歷（Traversal）**：從頭到尾依序訪問每個元素，通常搭配迴圈

```
對 索引 從 0 到 4:
    印出 "第" + (索引+1) + "天產量：" + 每日產量[索引]
```

---

## 工業應用：每日產量紀錄

```
月產量紀錄 = [480, 512, 498, 467, 521, ...]  ← 共30個值

月總量   = 加總(月產量紀錄)
月平均   = 月總量 / 30
最高產日 = 找最大值的索引(月產量紀錄) + 1
```

**感測器時序資料**：每 10 秒記錄溫度，一天 8,640 筆資料，可用迴圈掃描異常時段

---

# 八、堆疊與佇列（Stack & Queue）

堆疊與佇列都是管理「一序列資料」的結構，但規定資料進出的順序截然不同。

本章介紹 LIFO 與 FIFO 兩種原則，以及各自最適合的工業應用場景。

---

## 堆疊（Stack）——後進先出（LIFO）

遵循 **LIFO（Last In, First Out）**：最後放入的資料，最先被取出

想像一疊盤子：只能從最頂端 **push**（放入）或 **pop**（取出）

```
Push(C)     Pop()
   [C] ←─────────────────→ [C] 被取出
   [B]                        [B]
   [A]                        [A]
```

---

## 趣味小知識：push/pop 的由來

- 據說源自早期美式自助餐廳的彈簧式疊盤機
- 把盤子往下壓（push），彈簧頂住；抽走一個（pop），彈簧頂高一階
- 1950年代電腦科學家借用這個生活裝置命名堆疊操作
- 直接借用日常經驗，比憑空發明術語更容易理解

---

## 工業應用：流程回溯與步驟檢查

**復原（Undo）**：每次操作推入堆疊，復原時從頂端彈出並反向執行

**設備啟動/關閉程序**：關閉須以相反順序（後啟動的先關閉）

```
啟動（Push）：冷卻系統 → 液壓系統 → 主軸馬達
關閉（Pop，後進先出）：
  先關 主軸馬達（最後啟動，最先關閉）
  再關 液壓系統
  最後關 冷卻系統
```

---

## 佇列（Queue）——先進先出（FIFO）

遵循 **FIFO（First In, First Out）**：最先放入的資料，最先被取出——正是排隊的邏輯

```
入隊方向 →  [A][B][C]  → 出隊方向

Enqueue(D)：[A][B][C][D]
Dequeue()： A 被取出，剩 [B][C][D]
```

- **Enqueue（入隊）**：加入佇列尾端
- **Dequeue（出隊）**：取出並移除佇列頭部

---

## 工業應用：生產排程與等候加工

佇列是所有 **排隊等候系統** 的天然模型：

- **生產排程**：工單依下達時間排入佇列，機台空出時取出下一張
- **訂單處理**：客戶訂單依收到時間排入佇列
- **列印排隊**：多人列印時依序處理

---

## 小結：堆疊與佇列

> **工廠中的「等候加工隊列」是佇列；「操作步驟的逆序回溯」是堆疊**

- 理解這兩種結構，能幫助你為不同的等候或順序問題選擇正確的資料模型

---

# 九、集合與映射（Set / Map）

集合強調「不重複」，映射強調「快速查值」——兩者都是工業資料管理的核心工具。

本章介紹兩種資料結構的特性與典型應用。

---

## 集合（Set）——唯一性

只儲存 **不重複元素**，加入相同的值會被自動忽略

```
已出現的不良原因 = {}
加入 "尺寸超差"  → {"尺寸超差"}
加入 "尺寸超差"  → {"尺寸超差"}  ← 重複，不加入
加入 "缺料"      → {"尺寸超差", "缺料"}
```

**工業應用**：快速找出「本月共出現了哪些不良類型」，不用逐一去除重複值

---

## 映射（Map）——鍵值對

又稱字典（Dictionary），內部常以 **雜湊表（Hash Table）** 實作，儲存 **鍵值對（Key-Value Pair）**

```
設備狀態表 = {
    "M-001": "運作中",
    "M-002": "維修中"
}

查詢：設備狀態表["M-002"]  → "維修中"
```

> **依鍵查值的速度極快，不論資料多少，查詢時間幾乎固定——這與逐一搜尋的列表相比效能差異顯著**

---

## 工業應用：物料編號查詢

- **物料主檔**：`{"MAT-001": {"名稱": "齒輪", "安全庫存": 100}}`
- **設備對應表**：`{"機台編號": "負責技術員姓名"}`
- **產品規格表**：`{"產品料號": {"長度": 50.0, "公差": 0.05}}`

---

# 十、樹與層級結構（概念性介紹）

許多資料天然具有層級關係，例如組織架構、產品零件組成。

本章介紹樹狀結構的基本術語，並以 BOM（產品物料清單）作為製造業最重要的應用案例。

---

## 什麼是樹？

前面的列表、堆疊、佇列都是 **線性** 結構；樹則表達 **層級（Hierarchical）** 關係

```
              總經理
             /      \
        製造部       業務部
       /      \
    製造一課  製造二課
```

- **根節點（Root）**：最頂端節點
- **父節點**：有子節點的節點
- **葉節點（Leaf）**：沒有子節點的節點

---

## 工業應用：產品結構（BOM）

**產品物料清單（BOM）** 描述成品由哪些零組件組成，形成層級分明的樹狀結構

```
成品：自行車
├── 車架總成（主車架、前叉）
├── 傳動系統（踏板組、鏈條、齒輪盤）
└── 輪組（×2）（輪圈、輪胎、輻條）
```

> **ERP 計算生產需要多少原材料時，就是沿著 BOM 樹向下展開，逐層計算需求量——這個過程稱為 MRP 展算**

---

## 小結：樹狀結構

> **樹狀結構是表達「整體—部分」或「上層—下層」關係的最自然工具**

- 組織架構、設備層級（工廠→產線→機台→模組）、產品 BOM、WBS 都是樹狀結構的體現
- 理解樹的概念，有助於理解 ERP/MES 系統中許多資料組織的方式

---

# 第三篇：演算法基礎與效率概念

---

# 十一、演算法基本概念

演算法是解決特定問題的一系列明確、有限、有序的步驟。

本章深入演算法的特性，並釐清演算法與程式之間「概念」與「實作」的關係。

---

## 演算法的定義與特性

一個好的演算法必須具備：

- **明確性（Definiteness）**：每步驟清楚無歧義
- **有限性（Finiteness）**：有限步驟後終止
- **輸入（Input）**：零個或多個明確定義的輸入
- **輸出（Output）**：一個或多個明確定義的輸出
- **有效性（Effectiveness）**：每步驟都可實際執行

---

## 演算法與程式的關係

演算法是 **概念層面的解題步驟**，程式則是演算法的 **具體實作**

```
問題：在一個列表中，找出最大值

演算法（語言無關）：
  1. 將「目前最大值」設為列表第一個元素
  2. 逐一與「目前最大值」比較，較大則更新
  3. 重複直到所有元素比較完畢
  4. 回傳「目前最大值」
```

---

## 工業應用：SOP 的演算法本質

| 演算法特性 | SOP 對應 |
|---|---|
| 明確性 | 每個操作步驟清楚描述 |
| 有限性 | 作業有明確開始與結束 |
| 輸入 | 原料、半成品、設備參數 |
| 輸出 | 成品、品質量測結果 |

> **將 SOP 轉化為電腦可執行的演算法，正是「流程數位化與自動化」的核心工作**

---

# 十二、基本演算法範例

搜尋與排序，是程式中最頻繁使用的兩類演算法。

本章以線性搜尋、泡沫排序為例，建立對演算法效率的直覺理解。

---

## 搜尋：線性搜尋（Linear Search）

從頭到尾逐一檢查每個元素，直到找到目標或確認不存在

```
目標：找出機台 "M-003" 的位置
機台清單 = ["M-001", "M-002", "M-003", "M-004", "M-005"]

步驟1：檢查 "M-001"，不是目標，繼續
步驟2：檢查 "M-002"，不是目標，繼續
步驟3：檢查 "M-003"，找到！→ 回傳索引 2
```

> **優點是邏輯簡單、不需事先排序；缺點是資料量大時效率較低**

---

## 排序：泡沫排序（Bubble Sort）

反覆比較相鄰元素、交換位置，逐漸將資料調整到正確位置

```
初始：[64, 34, 25, 12, 22]
第一輪：[34, 25, 12, 22, 64]  ← 64 沉到最右
第二輪：[25, 12, 22, 34, 64]  ← 34 沉到倒數第二
...
最終：[12, 22, 25, 34, 64]
```

> **重點在流程不在細節：排序是透過反覆比較與交換，逐漸將資料調整到正確位置**

---

## 工業應用：排序的用途

- 將工單依「截止日期」排序，確保緊急工單優先處理
- 將不良品依「不良次數由多到少」排序（柏拉圖分析的基礎）
- 將設備依「OEE 數值由低到高」排序，找出最需改善的機台

---

![bg right:45%](images/03-Computer-Program/tony-hoare.jpg)

## 趣味小知識：快速排序的發明

- 泡沫排序直觀但非實務常用——真正撐起現代排序功能的是 **快速排序（Quicksort）**
- 1959年由英國電腦科學家東尼·霍爾發明
- 他當時在莫斯科國立大學當訪問學生，原本只想把俄文詞典排序
- 意外發明了影響後世超過一甲子的演算法

---

# 十三、效率與時間複雜度概念

同一個問題可以用不同演算法解決，但效率可能天差地別。

本章建立時間複雜度與大 O 符號的直覺理解，這是評估系統效能的基礎工具。

---

## 為何需要效率分析？

**工業情境**：工廠有 1,000 筆庫存品項，搜尋只要 0.001 秒；增長到 1,000,000 筆，需要 1 秒甚至更長

> **規模增大時，效率的差異會被放大到無法忽視**

---

![bg left:45%](images/03-Computer-Program/donald-knuth.jpg)

## 時間複雜度與大 O 符號

- **時間複雜度**：輸入資料量增加時，執行時間如何成長
- **大 O 符號**：描述「最壞情況下的成長趨勢」
- 大 O 符號最早由數論學家提出，真正帶進電腦科學的是唐納·克努斯
- 他的巨著《電腦程式設計的藝術》至今仍是演算法領域經典

---

## O(1)——常數時間

無論 n 多大，執行時間 **固定不變**

```
每日產量[3] → 無論列表有 5 個還是 5,000 個元素，
              存取第 4 個元素的時間完全相同
```

**工廠類比**：倉庫有固定編號儲位，直接走過去拿，不需搜尋

---

## O(n)——線性時間

執行時間 **與 n 成正比**：資料量變 2 倍，時間也變 2 倍

```
在 100 筆資料中搜尋 → 最多 100 次比較
在 10,000 筆資料中搜尋 → 最多 10,000 次比較
```

**工廠類比**：倉庫沒有分區，只能從頭逐一尋找，零件越多越花時間

---

## O(n²)——平方時間

執行時間 **與 n 的平方成正比**：資料量變 2 倍，時間變 4 倍

```
排序 10 筆資料    → 約 100 次比較
排序 1,000 筆資料 → 約 1,000,000 次比較
```

當 n 不大時勉強可接受，但一旦 n 變大，效能就會急速惡化

---

## 直覺式比較

假設每次基本操作花 1 微秒：

| n（資料量） | O(1) | O(n) | O(n²) |
|---|---|---|---|
| 10 | 1 μs | 10 μs | 100 μs |
| 1,000 | 1 μs | 1 ms | 1 秒 |
| 1,000,000 | 1 μs | 1 秒 | **11.6 天** |

> **百萬筆資料時，O(1) 瞬間完成，O(n) 需要1秒，O(n²) 需要將近兩週**

---

# 十四、工業情境中的效率思考

演算法效率的概念，與工業工程中的「瓶頸」概念高度相似。

本章把效率分析連結到限制理論、排程優化與資料量增長對決策速度的影響。

---

## 生產線瓶頸類比

> **整體產出受限於最慢的工站——改善非瓶頸工站幾乎沒有幫助，唯有改善瓶頸，整體才能提升**

```
工站A：100件/分　工站B：30件/分 ← 瓶頸　工站C：80件/分
整體產出受限於工站B，提升A到200件/分，整體仍是30件/分
```

- 程式效能最佳化：優先找出並改善「效能瓶頸」，而非平均微調每個部分

---

## 排程效率 vs 計算效率

生產排程本質：在工單、機台、工人、截止日期限制下找最佳安排

```
10 張工單的排列數：10! = 3,628,800 種
20 張工單的排列數：20! ≈ 2.4 × 10¹⁸ 種
```

> **這種現象稱為「組合爆炸」——實際工廠不窮舉所有可能，而是採用啟發式演算法，找「夠好的解」**

---

## 資料量增加對決策時間的影響

「資料多」不等於「決策快」——演算法效率不佳，資料量增加反而讓系統變慢：

- **即時品質判斷**：視覺檢測若複雜度過高，跟不上生產節拍
- **預測性維護**：模型訓練時間若超過資料更新週期，永遠追不上
- **供應鏈優化**：複雜度超線性增長，須採用近似演算法或分散式計算

---

## 小結：效率分析的價值

> **理解 O(1)、O(n)、O(n²) 的直覺含義，能幫助你判斷「為什麼系統越來越慢」這類問題**

- 效率分析不是純學術課題，工業工程師設計或評估資訊系統時同樣用得上

---

# 第四篇：程式設計與工業應用整合

---

# 十五、資料驅動的流程改善

「資料驅動」是工業4.0的核心理念之一：決策不再單純依賴經驗，而是基於客觀資料分析。

本章示範如何用簡單程式邏輯，從生產資料中找出異常與趨勢。

---

## 用簡單程式分析生產資料

```
良率記錄 = [0.976, 0.981, 0.968, 0.943, 0.972, ...]

分析步驟：
1. 計算月平均良率 → 0.962
2. 找出低於目標的日期 → [第4天:0.943, 第9天:0.924]
3. 計算低於目標天數佔比 → 2/30 = 6.7%
```

> **手動分析需要幾分鐘，寫成程式只需不到1秒，且能每天自動執行**

---

## 找出異常與趨勢

**異常偵測（Anomaly Detection）**：設定正常範圍，自動標記超出範圍的資料點

```
上限 = 25.0°C　下限 = 20.0°C
溫度資料 = [22.1, 23.4, 21.8, 26.2, 22.9, 19.8, 23.1]

結果：過高異常 26.2°C（第4筆）　過低異常 19.8°C（第6筆）
```

**趨勢分析**：觀察資料隨時間的變化方向，在問題爆發前提供預警

---

# 十六、自動化思維

自動化是工業4.0三大核心特徵之一：讓更多原本需要人工執行的步驟，改由程式自動完成。

本章說明哪些工作適合自動化，並以生產報表為例展示完整流程。

---

## 適合自動化的工作特徵

- **重複性高**：每天、每週、每批次都要做一遍
- **規則明確**：步驟固定，判斷標準清楚
- **資料量大**：人工處理費時費力，且容易出錯
- **時效性要求高**：需要在特定時間內完成

---

## 工業應用：報表自動生成

```
1. 班次結束（觸發條件）
2. 自動連線 MES 資料庫
3. 查詢今日各機台的產量、良率、稼動率
4. 計算各項 KPI（OEE、達成率、不良率）
5. 儲存為 PDF 與 Excel，自動寄送相關人員
```

> **以往：班長花 30–60 分鐘手動整理；自動化後：程式 5 秒完成**

---

# 十七、工業4.0概念導入

工業4.0的核心價值鏈，可以用「資料→資訊→決策」三個層次描述。

本章串連感測器、程式與決策邏輯，並以設備振動監測作為完整案例。

---

## 資料 → 資訊 → 決策

```
感測器原始資料（溫度23.5、壓力101.2、振動0.03）
         ↓  資料處理與分析程式
有意義的資訊（主軸溫度正常、液壓壓力偏低）
         ↓  決策邏輯
可執行的決策（發出預警，通知技術員檢查）
```

> **程式扮演「資料」到「資訊」轉化過程的核心角色**

---

## IoT 資料如何進入程式處理流程

```
[感測器] → 類比訊號
[ADC 轉換] → 數位訊號
[PLC / 邊緣裝置] → 工業通訊協定（Modbus、OPC UA）
[資料擷取程式] → 數值資料
[資料處理程式] → 存資料庫／異常偵測／更新儀表板
```

- 每個箭頭之間都有程式在運作：擷取、轉換、驗證、儲存、分析

---

## 案例：設備振動監測

```
振動閾值 = 0.5　警告閾值 = 0.8　緊急閾值 = 1.2

無限迴圈（持續監測）:
    振動值 = 讀取感測器()
    如果 振動值 > 緊急閾值:
        執行緊急停機程序()
        break
    否則如果 振動值 > 警告閾值:
        連續異常計數 += 1
        如果 >= 5: 發送預警通知(技術員)
```

---

# 十八、系統觀點

程式不是孤立運作的，而是嵌入在硬體、網路、作業系統共同構成的系統中。

本章從系統整合的角度，回顧 ERP、MES 背後其實都是本教材介紹過的基礎概念。

---

## 程式 + 資料結構 + 硬體/系統整合

```
應用層：ERP、MES、SCADA、BI 儀表板
   ↑ 程式邏輯（演算法 + 資料結構）
平台層：資料庫、訊息佇列、API 服務
   ↑ 作業系統（資源管理、行程調度）
基礎設施層：伺服器、工業電腦、PLC、感測器
```

> **程式效能直接受下層硬體限制——理解硬體對工業工程師同樣重要**

---

## MES / ERP 背後的資料處理概念

**ERP 的物料需求規劃（MRP）**：BOM 樹的遍歷 + 庫存查詢（映射）+ 計算

**MES 的生產排程**：工單列表 + 優先序排列（排序）+ 機台分配（映射）

> **這些系統雖龐大，但核心邏輯都是迴圈、條件判斷、資料結構操作**

---

## 銜接提示

本篇（十五～十八）談的資料驅動、自動化、工業4.0資料流、ERP/MES 程式邏輯，會在全課程最後的智慧工廠案例中，與硬體、資訊系統、資料科學內容整合在一起。

屆時可以回頭對照這裡學過的概念。

---

# 第五篇：實作與應用建議

---

# 十九、基礎練習

本章提供三個資料計算練習與一個條件判斷練習，建議用 Excel、Python 或純邏輯描述來實作。

---

## 基礎練習主題

- **練習1：良率計算**——計算各機台良率、標示未達目標的機台
- **練習2：感測器統計**——24筆溫度記錄，計算最高、最低、平均與異常次數
- **練習3：庫存警示**——比較現有庫存與安全庫存，找出需補貨品項
- **練習4：尺寸分類**——依公差範圍將量測值分類為合格/過小/過大

---

# 二十、小型應用案例

三個綜合案例，把前面所學的資料結構與演算法應用在完整情境中。

---

## 小型應用案例主題

- **簡單庫存管理模擬**：入庫、出庫、庫存盤點報告（映射操作）
- **生產排程（Queue 模型）**：以 FIFO 原則排程工單，急件可插隊
- **生產資料統計報表**：計算良率、稼動率，找出最佳/最差班次

---

# 二十一、視覺化與呈現

資料分析的最後一步，往往是將結果視覺化，讓人更容易看出趨勢、比較差異。

本章整理常見圖表類型，並說明一張好的產量趨勢圖該包含哪些元素。

---

## 常見圖表類型

| 圖表類型 | 適合呈現 | 工業應用範例 |
|---|---|---|
| 折線圖 | 隨時間變化的趨勢 | 每日良率趨勢 |
| 柱狀圖 | 不同類別的比較 | 各機台產量比較 |
| 柏拉圖 | 頻率排名＋累計比例 | 不良原因分析 |
| 散布圖 | 兩變數的相關性 | 溫度與良率關係 |
| 管制圖 | 製程數據與管制界限 | SPC 品質管制 |

---

## 工業應用：產量趨勢圖要素

- **X 軸**：時間（日期、週次、月份）
- **Y 軸**：產量或良率數值
- **目標線**：清楚標示目標值
- **異常標記**：明顯標示低於目標的時間點
- **趨勢線**：顯示整體走向

> **好的圖表讓管理者幾秒鐘掌握狀況，遠比閱讀充滿數字的表格有效率**

---

# 二十二、學習重點總結

完成本課程後，你應該具備以下五項能力。

---

## 你應該具備的能力（一）

**運算思維與問題拆解**：能運用問題分解、模式辨識、抽象化、演算法思考，把複雜工業問題轉化為可執行步驟

**基本程式邏輯理解**：能閱讀並理解變數、條件判斷、迴圈、函數，與 IT 人員精確溝通系統需求

---

## 你應該具備的能力（二）

**資料結構的直覺理解**：理解列表、堆疊、佇列、映射、樹的特性與適用情境

**效率概念的應用**：用直覺理解 O(1)、O(n)、O(n²) 的差異，評估系統效能問題

**程式設計與工業流程的連結**：理解 MES、ERP 等系統背後的資料處理邏輯

---

## 給同學的話

> **程式語言的語法可以查手冊，但思考問題的方式、選擇合適資料結構的直覺、判斷系統效能瓶頸的能力，才是真正有價值、長期有效的核心素養**

- 本教材刻意避免語法細節與複雜數學推導
- 重點在建立「可應用於工業場域的計算思維」
- 建議搭配實際動手練習，理解才能真正落地
